# BuzzChat API Configuration
PORT=3001
NODE_ENV=production

# Admin secret for creating API keys
ADMIN_SECRET=change-this-to-a-secure-secret

# Anthropic API key for AI responses (optional - uses templates if not set)
ANTHROPIC_API_KEY=sk-ant-xxx

# Database (future - currently using in-memory)
# DATABASE_URL=postgresql://user:pass@localhost:5432/buzzchat

# Redis for rate limiting (future - currently using in-memory)
# REDIS_URL=redis://localhost:6379
